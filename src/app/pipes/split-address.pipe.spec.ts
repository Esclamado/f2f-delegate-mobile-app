import { SplitAddressPipe } from './split-address.pipe';

describe('SplitAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
